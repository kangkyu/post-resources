<h3>Welcome!</h3>

<% if current_user %>
  <%= link_to 'new category', new_category_path %>

  <% Category.all.each do |category| %>
  <ul>
    <li><%= category.name %></li>
  </ul>
  <% end %>

  <%= link_to 'new post', new_post_path %>
<% end %>

<%= render @posts %>
